# Use boolean expressions as appropriate on the lines below!

# Make me false!
bool_one = (2 <= 2) and "Alpha" == "Bravo"  # We did this one for you!

# Make me true!
bool_two = (1 > 0) or "Alpha" == "Bravo"

# Make me false!
bool_three = not (12 == 12)

# Make me true!
bool_four = ((10 % 4) == 2) or 15 >= 16

# Make me true!
bool_five = not not (12 == 12)
